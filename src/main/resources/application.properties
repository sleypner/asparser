
spring.application.name=parserarticles
info.app.name=Parser articles
info.app.description=app for parsing articles
info.app.version=1.1

spring.main.banner-mode=off
logging.level.root=warn
management.endpoints.web.exposure.include=info,health
management.info.env.enabled=true
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui-custom.html
spring.jpa.open-in-view=false

server.error.path=/error
server.error.whitelabel.enabled=false

#logging.level.org.springframework.security=DEBUG

#spring.jpa.properties.hibernate.format_sql=true
#logging.level.org.hibernate.SQL=DEBUG
#logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

frontend.url=http://localhost:8080
server.port=8080

google.recaptcha.secret=${GOOGLE_CAPTCHA}

db.url=${DB_CONNECT_URL}
db.username=${DB_CONNECT_USERNAME}
db.password=${DB_CONNECT_PASSWORD}

admin.username=${ADMIN_USERNAME}
admin.password=${ADMIN_PASSWORD}

parse.zone=${PARSE_TIME_ZONE}
parse.articles.cron=${PARSE_ARTICLES_CRONE_EX}
parse.fortress.cron=${PARSE_FORTRESS_CRONE_EX}
parse.online.delay=${PARSE_ONLINE_DELAY}
parse.online.initial-delay=${PARSE_ONLINE_DELAY_INIT}
parse.events.delay=${PARSE_EVENTS_DELAY}
parse.events.initial-delay=${PARSE_EVENTS_DELAY_INIT}


#spring.mail.host=smtp-mail.outlook.com
#spring.mail.port=587
#spring.mail.username=${O_USER}
#spring.mail.password=${O_PASS}
#spring.mail.properties.mail.smtp.auth=true
#spring.mail.properties.mail.smtp.starttls.enable=true

#Google smtp
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${G_APP_USER}
spring.mail.password=${G_APP_PASS}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
#spring.mail.properties.mail.ssl.trust=true

smtp.mail.from=${G_APP_USER}